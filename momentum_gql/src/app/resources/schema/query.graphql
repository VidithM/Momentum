# Query definitions

type Query {
    about: AboutPayload

    search_users(
        terms: SearchUsersInput
    ): UsersPayload

    search_comments(
        terms: SearchCommentsInput
    ): CommentsPayload

    search_communities(
        terms: SearchCommunitiesInput
    ): CommunitiesPayload

    search_posts(
        terms: SearchPostsInput
    ): PostsPayload
}
